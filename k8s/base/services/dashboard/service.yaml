apiVersion: v1
kind: Service
metadata:
  name: mail-queue-dashboard
  namespace: mail-queue
  labels:
    app.kubernetes.io/name: mail-queue
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mail-queue
    app.kubernetes.io/component: dashboard
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
